// Stable re-export: @dsh-plugin/dsh-loader/timeout
//
// Timer / deadline helpers (MAX_TIMER_DELAY_MS, deadline).
// When dsh renames the package, only this file changes.
export * from '@deepseek-ai/dsh-timeout';