// Stable re-export: @dsh-plugin/dsh-loader/credentials
//
// Credential references (credentialRef).
// When dsh renames the package, only this file changes.
export * from '@deepseek-ai/dsh-credentials';