# Dependencies
node_modules/

# Lockfile: CI 设计为无锁文件安装（npm install），且本机生成的锁文件会
# 混入工作区 pnpm store 的相对路径，禁止提交。
package-lock.json

# Build output
lib/
dist/

# Examples: better-sidebar lives in its own repo
examples/dsh-better-sidebar-loader/

# OS / editor
.DS_Store
*.swp
*.swo
*~

# Logs
*.log
